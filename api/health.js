const { client, json, fail } = require('../lib/supabase'); module.exports = async (req, res) => { if (req.method !== 'GET') return json(res, 405, { error: 'Method not allowed.' }); try { const { error } = await client().from('questions').select('id').limit(1); if (error) throw error; return json(res, 200, { ok: true, database: 'supabase' }); } catch (error) { return fail(res, error); } };
